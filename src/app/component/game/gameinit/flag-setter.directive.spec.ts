import { FlagSetterDirective } from './flag-setter.directive';

describe('FlagSetterDirective', () => {
  it('should create an instance', () => {
    const directive = new FlagSetterDirective();
    expect(directive).toBeTruthy();
  });
});
